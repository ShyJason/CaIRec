# Legacy Scripts

These scripts are preserved for historical ablations and exploratory experiments.

They are not part of the current MMRec mainline workflow. Use the top-level
entrypoints documented in `README.md` for the supported path.

Archived here:

- joint-training experiments
- long-cycle reporting
- stage1 parameter grid search
- task-aware/stage3 exploratory runs
- older no-imputation ablation wrapper
