# Best Hyperparameters By Dataset

The active per-dataset mainline table now lives in:

```text
docs/MAINLINE.md
```

This file is kept only as a compatibility pointer. Do not add new best-result
records here; add traceable results to `docs/MAINLINE.md` instead.
